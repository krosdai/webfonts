# Dependencies
node_modules
**/node_modules

# Build outputs
artifacts
build
coverage
dist
*.tsbuildinfo

# Logs
*.log

# Environment files
.env
.env.local
.env.*.local

# OS
.DS_Store

# Local config files
*.local.*

# Generated webfont assets — published to npm, regenerated by `pnpm build:fonts`, kept out of git.
# Only each package's package.json / README.md / OFL.txt are committed.
packages/**/*.woff2
packages/**/*.css
packages/**/metadata.json

# Font build caches (core binary, downloaded sources, previews)
.cache
.preview
.pnpm-store
